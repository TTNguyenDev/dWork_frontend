import { ModalStateType } from '../core/types';

export const ModalUtils: {
  storageDeposit: ModalStateType;
} = Object.freeze({
  storageDeposit: {
    onOpen: () => {},
    onClose: () => {},
  },
});
